// Temporarily disabled during migration to Turso
export function usePlayerStats() {
  return {
    players: [],
    loading: false,
    error: 'Hook disabled during migration to Turso cloud database'
  };
} 